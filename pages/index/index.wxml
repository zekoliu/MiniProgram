<view class='wather-wrapper'>
  <view class="temp">{{ nowTemp }}</view>
  <view class="txt">{{ nowWeather }}</view>
  <image class="weather-bg" src="{{ imageName}}" mode="scaleToFill"></image>
</view>